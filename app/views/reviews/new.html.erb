<%= render '/shared/banner' %>

<div class="container">

  <h2>Laisser un commentaire</h2>

  <div class="container">
  <%= simple_form_for ([ @maison, @review ]) do |f| %>
    <%= f.input :content %>
    <%= f.input :rating, collection: Review::AUTHORIZED_RATINGS,
                        include_blank: false %>
    <%= f.submit class: "btn btn-primary", value: "Valider" %>
    <%end %>
</div>


</div>
